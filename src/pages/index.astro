---
import Layout from '../layouts/Layout.astro'

let isDarkMode = true
let language = 'es'

// useEffect(() => {
//   document.documentElement.classList.toggle('dark', isDarkMode)
// }, [isDarkMode])

// const toggleTheme = () => {
//   setIsDarkMode(!isDarkMode)
// }

// const changeLanguage = (lang: string) => {
//   setLanguage(lang)
//   // Aquí se implementaría la lógica para cambiar el idioma en toda la aplicación
// }
---

<Layout title='Welcome to Astro.'>
  <!-- <TabsContent value='items'>
          <div
            class='grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4 mt-6'
          >
            {items.map((item: any) => <TooltipItem client:load item={item} />)}
          </div>
        </TabsContent> -->
</Layout>
